# InventoryCSharpSqlitePractice
A C# .NET Console application that interacts with an SQLite database using the basic CRUD operations for a POS system.

# What of SQLite I used
I've created tables. I've written code to insert, delete and return rows from the tables. So I've written code that performs Create-Read-Update-Delete operations.
